"use strict";
// Declaración de una variable de tipo string
let studentName = "Ana";
// Declaración de una variable de tipo number
let age = 28;
// Declaración de una variable de tipo boolean
let isStudent = true;
// Declaración de un array de strings
let courses = ["TypeScript", "Nest.js"];
// Declaración de una tupla (combinación de varios tipos en un array)
let person = ["Juan", 30];
// Muestra un mensaje combinando los valores de las variables
console.log(`${studentName} tiene ${age} años y está estudiando ${courses.join(", ")}`);
